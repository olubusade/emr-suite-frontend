
// Define the structure for a single monthly trend data point
export interface MonthlyTrend {
  month: string; // e.g., "January", "February"
  value: number; // The numeric value for that month (e.g., patient visits, revenue)
}
